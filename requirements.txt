# ControlPlane Checker - runtime dependencies (CPU-only).
#
# TARGET: Python 3.12 (verified on 3.12.3).
#
# This file contains ONLY what the implemented code actually needs: the proxy, the
# policy engine, the Layer 0/1 detectors, and the eval/optimizer path. The heavy ML
# stack (torch / transformers / sentence-transformers / Presidio / spaCy / Phoenix)
# lives in requirements-future.txt - those support detectors that are designed but not
# yet implemented (see ASSUMPTIONS.md). Keeping them out makes a clean install fast.
#
# Versions come from an actual pip-compile resolution of the full graph, then confirmed
# with `pip install --dry-run`.
#
# Install:  pip install -r requirements.txt

# --- Web / proxy ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
httpx==0.28.1
python-dotenv==1.2.3

# --- Provider abstraction (Groq via LiteLLM) ---
litellm==1.98.0

# --- Client used by the smoke test (proves base_url-only integration) ---
openai==2.54.0

# --- Policy-as-code ---
pyyaml==6.0.3

# --- Eval: PR curves, cost-weighted threshold optimizer, latency percentiles ---
scikit-learn==1.9.0
matplotlib==3.11.1
numpy==2.5.2
pandas==3.0.5

# --- Tests ---
pytest==9.1.1
pytest-asyncio==1.4.0
