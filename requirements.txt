# ControlPlane Checker — pinned, CPU-only dependencies (Python 3.11).
#
# Install:  pip install -r requirements.txt
# spaCy model (installed separately, CPU):
#           python -m spacy download en_core_web_sm
#
# NOTE: torch resolves to the CPU wheel by default on Windows/macOS PyPI.
#       On Linux, if a CUDA wheel is pulled, install the CPU build first:
#       pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cpu

# --- Web / proxy ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.8.2
pydantic-settings==2.4.0
httpx==0.27.0
sse-starlette==2.1.2
python-dotenv==1.0.1

# --- Provider abstraction ---
litellm==1.44.7

# --- NLI / groundedness / self-consistency (CPU) ---
sentence-transformers==3.0.1
transformers==4.43.3
torch==2.3.1
numpy==1.26.4

# --- PII / secrets ---
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
spacy==3.7.5

# --- Telemetry -> Arize Phoenix ---
opentelemetry-api==1.26.0
opentelemetry-sdk==1.26.0
opentelemetry-exporter-otlp-proto-http==1.26.0
opentelemetry-semantic-conventions==0.47b0
arize-phoenix==4.21.0

# --- Policy-as-code ---
pyyaml==6.0.1

# --- Eval / optimization ---
scikit-learn==1.5.1
pandas==2.2.2

# --- Operator console ---
streamlit==1.37.1

# --- Tests ---
pytest==8.3.2
pytest-asyncio==0.23.8
